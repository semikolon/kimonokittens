require_relative 'spec_helper'

RSpec.describe 'BankBuster', skip: 'Legacy Vessel dependency; disabled until migration completes' do
  it 'is temporarily skipped' do
    expect(true).to be true
  end
end
